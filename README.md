# Cypress E2E Tests - Fufelka.ru

E2E тесты для проверки авторизации и управления профилем пользователя на dev-среде https://fufelka.ru

## 📋 Покрытые сценарии

| # | Файл | Сценарий | Описание |
|---|------|----------|----------|
| 1 | `01-login.cy.js` | **Логин через UI** | Открытие страницы, ввод логин/пароль, ожидание auth-эндпоинта, проверка успешного входа |
| 2 | `02-session-persistence.cy.js` | **Проверка сохранения сессии** | Перезагрузка страницы, проверка что пользователь остаётся авторизованным, валидация токенов/куки |
| 3 | `03-profile-navigation.cy.js` | **Переход в профиль** | Открытие меню профиля через UI, проверка загрузки данных профиля и текущего аватара |
| 4 | `04-avatar-upload.cy.js` | **Смена аватарки** | Загрузка картинки из фикстур, ожидание upload-запроса, проверка изменения src аватара, проверка сохранения после reload |

## 🚀 Как запустить тесты

### Установка зависимостей

```bash
npm install
```

### Запуск тестов

**Headless режим (рекомендуется):**
```bash
npm run test:e2e
```

**Headed режим (с видимым браузером):**
```bash
npm run test:e2e:headed
```

**GUI режим (интерактивный):**
```bash
npm run cypress:open
```

> ⚠️ **Примечание:** На некоторых системах Windows GUI режим может не работать из-за проблем с Electron. Используйте headless режим.

### Запуск отдельного теста

```bash
npx cypress run --spec "cypress/e2e/01-login.cy.js"
npx cypress run --spec "cypress/e2e/02-session-persistence.cy.js"
npx cypress run --spec "cypress/e2e/03-profile-navigation.cy.js"
npx cypress run --spec "cypress/e2e/04-avatar-upload.cy.js"
```

## 📁 Структура проекта

```
cypress/
├── e2e/
│   ├── 01-login.cy.js              # Тест 1: Логин через UI
│   ├── 02-session-persistence.cy.js # Тест 2: Проверка сессии
│   ├── 03-profile-navigation.cy.js  # Тест 3: Переход в профиль
│   └── 04-avatar-upload.cy.js       # Тест 4: Смена аватарки
├── fixtures/
│   ├── avatar.jpg                   # Изображение для загрузки аватара
│   └── testData.json                # Тестовые данные
├── support/
│   ├── commands.js                  # Кастомные команды (login, navigate...)
│   └── e2e.js                       # Глобальная конфигурация, моки Telegram API
├── screenshots/                     # Скриншоты при ошибках
└── videos/                          # Видеозаписи прогонов
```

## 🔧 Конфигурация

Файл `cypress.config.js`:

```javascript
module.exports = defineConfig({
  e2e: {
    baseUrl: 'https://fufelka.ru',
    viewportWidth: 390,
    viewportHeight: 844,
    defaultCommandTimeout: 10000,
    video: true,
  },
});
```

## 📊 Автоматически генерируемые данные

- **Test Run ID** - уникальный идентификатор каждого запуска теста (timestamp + random string)
- **Логи cookies и localStorage** - автоматически логируются для отладки сессии

## ⚙️ Технические требования

✅ **Cypress ≥ 13** (используется 13.17.0)

✅ **Стабильные локаторы** - data-test, id, aria-label, PageObject-команды

✅ **Реальные действия пользователя** - авторизация и загрузка файла через UI

✅ **cy.intercept() + cy.wait('@alias')** - ожидания только через перехват запросов

✅ **Фикстуры** - cypress/fixtures/avatar.jpg для загрузки аватара

✅ **Конфигурация через cypress.config.js** - baseUrl, viewports, timeouts

## 🎬 Результаты

После запуска тестов:
- **Видео** сохраняются в `cypress/videos/`
- **Скриншоты** (при ошибках) в `cypress/screenshots/`

## 📝 Кастомные команды

| Команда | Описание |
|---------|----------|
| `cy.login(username, password)` | Выполняет логин через UI |
| `cy.navigateToProfileSettings()` | Переходит в настройки профиля |
| `cy.navigateToProfileEdit()` | Переходит на страницу редактирования профиля |
| `cy.getByDataTest(value)` | Получает элемент по data-test атрибуту |
| `cy.getByTestId(value)` | Получает элемент по data-testid атрибуту |
| `cy.uploadFile(selector, filePath)` | Загружает файл в input |
| `cy.getImageSrc(selector)` | Получает src атрибут изображения |

## 🔐 Особенности приложения

Приложение является **Telegram Mini App (TWA)**, поэтому:
- Мокируется `Telegram.WebApp` API в `e2e.js`
- Устанавливается секретный ключ `sk` в localStorage для доступа к dev-среде

## 👤 Тестовые учетные данные

- **Логин:** Ochko228
- **Пароль:** Zxcvbn
- **URL:** https://fufelka.ru/sign-in
